{% extends 'base.html.twig' %}

{% block title %}Portfolio - {{categorie.title}} {% endblock %}

{% block body %}
<a class="anchor bg-color" href="#top">
    <img src="{{ asset('assets/images/start-up.png')}}" alt="">
</a>

<div class="portfolio">
    <div class="header-page">
        <h1><span class="color">{{ categorie.title }}</span></h1>
    </div>
    <div class="back-btn">
        <a class="bg-color-hov" href="{{ path ('portfolio') }}">
            <img src="{{ asset('assets/images/icon-arrow.png')}}" alt="arrow">
            Retour
        </a>
    </div>
    {% for project in categorie.projects %}
        {% if project.portfolio %}
            <div class="container-projet" >
                <div class="projet-image" >
                    <img src="{{ vich_uploader_asset(project, 'projectFile') | imagine_filter('portfolio') }}" alt="portfolio-first">
                </div>
                <div class="projet-text" >
                    <h2><span class="color">{{ project.title }}</h2>
                    <p>{{ project.description }}</p>
                    <a href="{{ project.link }}" target="_blank">
                        <button class="btnPortfolio bg-color-hov">Voir le projet</button>
                    </a>
                </div>
            </div>
        {% endif %}
    {% endfor %}
</div>
{% endblock %}
